<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础App主题 -->
    <style name="Theme.JMComicNext" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/m3_primary</item>
        <item name="colorOnPrimary">@color/m3_on_primary</item>
        <item name="colorSurface">@color/m3_surface</item>
        <item name="colorOnSurface">@color/m3_on_surface</item>
        <item name="android:windowBackground">@color/m3_surface</item>
    </style>

    <!-- 三点菜单美化（仅圆角+阴影，移除弃用动画引用） -->
    <style name="PopupMenuStyle" parent="Widget.Material3.PopupMenu">
        <item name="android:background">@drawable/rounded_menu_bg</item>
        <item name="android:elevation">8dp</item>
    </style>
</resources>
