<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"> <!-- 新增这行命名空间 -->

    <data>
        <variable
            name="comic"
            type="com.jmcomic_next.lyqs.bean.ComicBean" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        app:cardCornerRadius="6dp"
        app:cardElevation="2dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- 漫画封面 -->
            <ImageView
                android:id="@+id/iv_comic_cover"
                android:layout_width="match_parent"
                android:layout_height="160dp"
                android:scaleType="centerCrop"
                android:src="@mipmap/ic_launcher" />

            <!-- 漫画名称 -->
            <TextView
                android:id="@+id/tv_comic_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="4dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:text="@{comic.name}"
                android:textSize="12sp"
                android:textColor="@color/black" />

            <!-- 作者 -->
            <TextView
                android:id="@+id/tv_author"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="4dp"
                android:paddingEnd="4dp"
                android:paddingBottom="4dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:text="@{comic.author}"
                android:textSize="10sp"
                android:textColor="@color/gray_666" />

            <!-- 更新时间 -->
            <TextView
                android:id="@+id/tv_update_time"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="4dp"
                android:paddingEnd="4dp"
                android:paddingBottom="4dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:text="@{comic.updateTime}"
                android:textSize="10sp"
                android:textColor="@color/gray_666" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
